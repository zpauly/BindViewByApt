apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(':bind-annotations')
    compile deps.javapoet
    compile deps.autoservice
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
